name: on_Linux_hw
  
on:
  push:
    branches:
      - master 

jobs:
        
  build:
    runs-on: ubuntu-latest
    
    steps:
    
      - name: Git clone our repo
        uses: actions/checkout@v1
        
      - name: Setup Ninja & 
        run : sudo apt-get install ninja-build 

      - run : sudo apt-get pkgconf
      - run : pkgconf --version
        
      - name: Setup SDL2-dev
        run: sudo apt-get install libsdl2-dev
      #- name: Setup SDL2
      #  run: sudo apt-get install libsdl2-2.0
      - name: build_act
        run: |

          mkdir -p build
          cd build
          cmake -GNinja ..
          ninja
          ctest 
          cmake --install .
          ##################
          cd ../tests/ex02/bin/
          pwd
          ls
          ./02-sdl-dynamic
          ./02-sdl-static
